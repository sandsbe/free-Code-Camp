function slasher(arr, howMany) {
  // 1. Create an array to take the chopped off elements
  var newArr = [];
  // 2. Run for loop until elements chopped off
  for (i = 0; i < howMany; i++) {
    newArr = arr.shift();
  }
  return arr;
}

slasher([1, 2, 3], 2);
